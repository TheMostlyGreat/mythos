/**
 * @mythos/core — the surface-agnostic generation engine.
 *
 * The JTBD jobs, LLM calls, Zod schemas, and the story bible live here. Every
 * surface (apps/web now, apps/mcp later) consumes this package; nothing in here
 * may import UI or framework code. Tests and BDD scenarios run against `core`.
 *
 * Empty on purpose — the first job (mythos.WR1: raw idea → structured concept)
 * gets built here against its BEHAVIOR-SPEC scenario.
 */

export {};
