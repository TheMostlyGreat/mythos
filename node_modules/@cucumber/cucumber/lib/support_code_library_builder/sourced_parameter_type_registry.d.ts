import { type ParameterType, ParameterTypeRegistry } from '@cucumber/cucumber-expressions';
import type { ILineAndUri } from '../types';
export declare class SourcedParameterTypeRegistry extends ParameterTypeRegistry {
    private parameterTypeToSource;
    defineSourcedParameterType(parameterType: ParameterType<unknown>, source: ILineAndUri, order: number): void;
    lookupSource(parameterType: ParameterType<unknown>): ILineAndUri & {
        order: number;
    };
}
