import { Envelope } from '@cucumber/messages';
import { Options } from './types';
export declare class JUnitXmlPrinter {
    private readonly write;
    private readonly query;
    private readonly builder;
    private readonly testClassName?;
    private readonly namingStrategy;
    constructor(options: Options, write: (content: string) => void);
    update(envelope: Envelope): void;
    private makeReport;
    private makeTestCases;
    private pickleComparator;
    private makeFailure;
}
