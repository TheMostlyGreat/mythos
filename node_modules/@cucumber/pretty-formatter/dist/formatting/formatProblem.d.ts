import type { Theme } from '../types';
import { ProblemType } from '../utils';
export declare function formatProblem(type: ProblemType, details: string, theme: Theme, stream: NodeJS.WritableStream): string;
