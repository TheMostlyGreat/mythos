import type { WriteStream } from 'node:tty';
export type InterferenceConfig = {
    mode: 'passthrough';
} | {
    mode: 'suppress';
    streams: ReadonlyArray<WriteStream>;
};
export declare class InterferenceInterceptor {
    private readonly config;
    private readonly acquired;
    private writing;
    constructor(config?: InterferenceConfig);
    acquire(): void;
    bypass<T>(callback: () => T): T;
    release(): void;
}
