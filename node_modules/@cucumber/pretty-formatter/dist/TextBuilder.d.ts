import type { Style } from './types';
export declare class TextBuilder {
    private readonly stream;
    private text;
    constructor(stream: NodeJS.WritableStream);
    private applyStyle;
    space(): this;
    line(): this;
    append(value: string | number, style?: Style): this;
    build(style?: Style, styleEachLine?: boolean): string;
}
