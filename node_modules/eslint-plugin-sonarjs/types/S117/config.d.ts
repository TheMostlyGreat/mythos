export declare const fields: [[{
    readonly field: "format";
    readonly default: "^[_$A-Za-z][$A-Za-z0-9]*$|^[_$A-Z][_$A-Z0-9]+$";
    readonly description: "Regular expression used to check the names against.";
}]];
